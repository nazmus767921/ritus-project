### Task Overview
I need to fix a minor issue. its not a bug. its a logic issue. ad also need some improvements and add new features.
---


issue: the issue is after selling a item. if i made mistake and go edit the transactio adnn select aother stock item the description is updated with the new stock item. and the customer name ad note is gone because of replaced by the ew description. so i thik we should add the customer name and notes to the schema.



improvement: The Edit_Trasaction dialog currently lets user to change the transaction type INcome to expense and vise versa. But maybe it would be better if its not allowed. what do you suggest?

Feature: The Total available stock and the total remaining stock and total sold quantity should be calculated and show in the metrics.

Complete Revamp Improvements: The Import_shipment dialog's shipment items should be collapsible. one can be opened at a time. The collapsed item should show the important Input data.  when the collapse open it should show the form. The Item heder should be like this #1 [Brand]        ^. when user add row the new row should be like this #2 [Brand]        v. adn opened automatically. Another thing is currently the use fill up the form and submit it. but i want a review feature. after the user fill up the form and click confirm button. it should open a review dialog. The review dialog should show all the  the stock items in a list (no collapsible no form. just a list). The review should have Edit & Remove options for each stock item.  after user click confirm button it should open a confirmation dialog. The confirmation dialog should show the total quantity of stock items and total quantity of stock items to be imported. if user click confirm then the stock should be imported.


improvement: the mascot and chat taking to much space i need it to be compact. and chat bubble like at the top.
